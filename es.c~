#include <stdio.h>
#include <stdlib.h>

void f(size_t a){
  printf("%ld",a);
}
int main()
{
  /* size_t a=2;
  a+=1;cf
  f(a);*/
  char c[20];
   long long int  i=1234579098731;
 long long int  n=sprintf(c,"lld",i);
  printf("%lld",n);
  
  return 0;
}
